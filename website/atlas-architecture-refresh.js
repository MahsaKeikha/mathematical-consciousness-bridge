(() => {
  // Intentionally no runtime Atlas mutations.
  // The Visual Atlas keeps its canonical HTML/CSS layout and figure sizing.
  // Research III figure corrections must happen at their canonical source,
  // not by overriding Atlas presentation behavior at runtime.
})();
