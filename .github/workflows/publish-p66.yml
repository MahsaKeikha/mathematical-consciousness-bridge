name: publish-p66

on:
  push:
    branches:
      - p66_residual_exact_augmentation
    paths:
      - scripts/publish_p66.py
      - .github/workflows/publish-p66.yml

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: p66_residual_exact_augmentation
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Integrate P66 publication record
        run: python scripts/publish_p66.py
      - name: Commit publication integration
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md docs/theorem_roadmap.md docs/research_navigation.md docs/equation_and_citation_map.md pyproject.toml CITATION.cff CHANGELOG.md website/index.html website/research-map.html tests/test_release_metadata_consistency.py tests/test_research_website_integrity.py
          git commit -m "Publish P66 across public research record"
          git push origin HEAD:p66_residual_exact_augmentation
