name: integrate-p45-publication-once

on:
  push:
    branches:
      - p45-shared-preparation-graph-allocation

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: p45-shared-preparation-graph-allocation
          fetch-depth: 0

      - name: Apply P45 publication integration
        run: python .github/integrate_p45_publication.py

      - name: Remove one-time integration scaffolding
        run: |
          rm .github/integrate_p45_publication.py
          rm .github/workflows/integrate-p45-publication-once.yml

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install project
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Validate publication state
        run: |
          pytest
          ruff check .

      - name: Commit verified P45 publication state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md CHANGELOG.md CITATION.cff pyproject.toml docs tests .github
          git commit -m "Integrate P45 into public research paper"
          git push origin p45-shared-preparation-graph-allocation
